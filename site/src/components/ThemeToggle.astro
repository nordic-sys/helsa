---
/**
 * Light/dark toggle for the hand-built pages.
 *
 * It deliberately reads and writes the same localStorage key as Starlight
 * (`starlight-theme`) and sets the same `data-theme` attribute, so a visitor
 * who switches the theme on the landing page finds the documentation already
 * switched — and the other way round. The two shells share a preference rather
 * than each keeping their own.
 *
 * Starlight's own control also offers "auto". This one cycles only light and
 * dark; writing either value is something Starlight's picker understands.
 */
---

<button class="theme-toggle" type="button" aria-live="polite" data-theme-toggle>
  <svg class="icon sun" viewBox="0 0 24 24" width="18" height="18" aria-hidden="true"
       fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round">
    <circle cx="12" cy="12" r="4" />
    <path d="M12 2v2M12 20v2M4.9 4.9l1.4 1.4M17.7 17.7l1.4 1.4M2 12h2M20 12h2M4.9 19.1l1.4-1.4M17.7 6.3l1.4-1.4" />
  </svg>
  <svg class="icon moon" viewBox="0 0 24 24" width="18" height="18" aria-hidden="true"
       fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
    <path d="M21 12.8A9 9 0 1 1 11.2 3a7 7 0 0 0 9.8 9.8Z" />
  </svg>
  <span class="sr-only" data-theme-label>Switch theme</span>
</button>

<script is:inline>
  document.querySelectorAll('[data-theme-toggle]').forEach((button) => {
    button.addEventListener('click', () => {
      const next = document.documentElement.dataset.theme === 'dark' ? 'light' : 'dark';
      document.documentElement.dataset.theme = next;
      try {
        localStorage.setItem('starlight-theme', next);
      } catch {
        /* Private browsing refuses the write; the page still switches. */
      }
      document.querySelectorAll('[data-theme-label]').forEach((label) => {
        label.textContent = next === 'dark' ? 'Switch to light theme' : 'Switch to dark theme';
      });
    });
  });
</script>

<style>
  .theme-toggle {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 2.25rem;
    height: 2.25rem;
    padding: 0;
    border: 1px solid var(--helsa-border);
    border-radius: 999px;
    background: transparent;
    color: var(--helsa-ink-muted);
    cursor: pointer;
    transition: color 0.15s, border-color 0.15s, background-color 0.15s;
  }
  .theme-toggle:hover {
    color: var(--helsa-ink);
    border-color: var(--helsa-border-strong);
    background: var(--helsa-surface);
  }
  .theme-toggle:focus-visible {
    outline: 2px solid var(--helsa-brand);
    outline-offset: 2px;
  }
  /* One button, two glyphs: show the theme you would switch *to*. */
  .icon { display: none; }
  :global(:root[data-theme='dark']) .sun { display: block; }
  :global(:root[data-theme='light']) .moon { display: block; }

  .sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip-path: inset(50%);
    white-space: nowrap;
    border: 0;
  }
</style>
